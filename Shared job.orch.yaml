type: "orchestration"
version: "1.0"
pipeline:
  components:
    Start:
      type: "start"
      transitions:
        unconditional:
        - "Run Shared Pipeline"
      parameters:
        componentName: "Start"
    Run Shared Pipeline:
      type: "run-shared-pipeline"
      parameters:
        componentName: "Run Shared Pipeline"
        pipelineName: "Matillion_DPC_Training#001-Load_log_insert"
        version: "[Latest]"
        setScalarVariables:
        setGridVariables:
      postProcessing:
        updateOutputMessage:
        updateScalarVariables:
design:
  components:
    Start:
      position:
        x: -250
        "y": 0
      tempMetlId: 1
    Run Shared Pipeline:
      position:
        x: -90
        "y": 0
      tempMetlId: 2
