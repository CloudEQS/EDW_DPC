type: "transformation"
version: "1.0"
pipeline:
  components:
    SQL:
      type: "sql"
      parameters:
        componentName: "SQL"
        query: "select  * from SNOWFLAKE_SAMPLE_DATA.TPCH_SF10.PARTSUPP"
    Rewrite Table:
      type: "rewrite-table"
      sources:
      - "SQL"
      parameters:
        componentName: "Rewrite Table"
        warehouse: "[Environment Default]"
        database: "SCD_PRACTICE_DB"
        schema: "PUBLIC"
        targetTable: "part2 "
        orderBy:
      postProcessing:
        updateOutputMessage:
        updateScalarVariables:
design:
  components:
    SQL:
      position:
        x: -210
        "y": -60
      tempMetlId: 1
    Rewrite Table:
      position:
        x: -50
        "y": -60
      tempMetlId: 2
