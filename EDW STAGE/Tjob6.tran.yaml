type: "transformation"
version: "1.0"
pipeline:
  components:
    SQL:
      type: "sql"
      parameters:
        componentName: "SQL"
        query: "select  * from  SNOWFLAKE_SAMPLE_DATA.TPCH_SF10.ORDERS"
    ORDER 2 TARGET  6:
      type: "rewrite-table"
      sources:
      - "SQL"
      parameters:
        componentName: "ORDER 2 TARGET  6"
        warehouse: "[Environment Default]"
        database: "SCD_PRACTICE_DB"
        schema: "PUBLIC"
        targetTable: "order2"
        orderBy:
      postProcessing:
        updateOutputMessage:
        updateScalarVariables:
design:
  components:
    SQL:
      position:
        x: -400
        "y": -20
      tempMetlId: 1
    ORDER 2 TARGET  6:
      position:
        x: -240
        "y": -20
      tempMetlId: 2
