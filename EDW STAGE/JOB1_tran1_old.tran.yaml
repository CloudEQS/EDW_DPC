type: "transformation"
version: "1.0"
pipeline:
  components:
    CUSTOMER:
      type: "table-input"
      parameters:
        componentName: "CUSTOMER"
        database: "SNOWFLAKE_SAMPLE_DATA"
        schema: "TPCH_SF1"
        targetTable: "CUSTOMER"
        columnNames:
        - "C_CUSTKEY"
        - "C_NAME"
        - "C_ADDRESS"
        - "C_NATIONKEY"
        - "C_PHONE"
        - "C_ACCTBAL"
        - "C_MKTSEGMENT"
        - "C_COMMENT"
        timeOffset:
    Rewrite Table:
      type: "rewrite-table"
      sources:
      - "CUSTOMER"
      parameters:
        componentName: "Rewrite Table"
        warehouse: "[Environment Default]"
        database: "EDW_LAB"
        schema: "PUBLIC"
        targetTable: "STAGE_CUSTOMER_TABLE"
        orderBy:
      postProcessing:
        updateOutputMessage: "CUSTOMER DATA LOAD INTO TARGET TABLE"
        updateScalarVariables:
design:
  components:
    CUSTOMER:
      position:
        x: -170
        "y": -10
      tempMetlId: 1
    Rewrite Table:
      position:
        x: -10
        "y": -10
      tempMetlId: 2
