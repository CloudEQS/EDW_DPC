type: "transformation"
version: "1.0"
pipeline:
  components:
    SQL:
      type: "sql"
      parameters:
        componentName: "SQL"
        query: "select * from SNOWFLAKE_SAMPLE_DATA.TPCDS_SF100TCL.INVENTORY"
    Rewrite Table:
      type: "rewrite-table"
      sources:
      - "SQL"
      parameters:
        componentName: "Rewrite Table"
        warehouse: "[Environment Default]"
        database: "INTERNAL_TRAINING"
        schema: "DBT_MKHAMBADKAR"
        targetTable: "TransfromationJob11"
        orderBy:
      postProcessing:
        updateOutputMessage:
        updateScalarVariables:
design:
  components:
    SQL:
      position:
        x: -70
        "y": -50
      tempMetlId: 1
    Rewrite Table:
      position:
        x: 120
        "y": -50
      tempMetlId: 2
